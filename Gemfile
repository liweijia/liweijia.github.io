source 'https://rubygems.org'

# 核心修改：移除 github-pages，直接使用新版 Jekyll (v4)
# 这会自动下载兼容 Ruby 3.1 的最新版 bibtex 插件
gem 'jekyll', '~> 4.3'

# 必须添加这个，Ruby 3.0+ 环境需要它
gem 'webrick'

group :jekyll_plugins do
    # 这些是你原来的插件，保持不变
    gem 'jekyll-email-protect'
    gem 'jekyll-github-metadata'
    gem 'jekyll-paginate-v2'
    gem 'jekyll-scholar'
    gem 'jekyll-twitter-plugin'
    gem 'jemoji'
    gem 'unicode_utils'
end
